#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;
int main()
{
		double n, a, b, x, s, d;
		cout << "n=";
		cin >> n;
		while (n > 0)
		{
			cout << "x=";
			cin >> x;
			s = ((sin(x) + tan(x) / (sqrt((1 / x) + 3 * x))));
			n--;
			cout << s << endl;
		}
	return 0;
}
